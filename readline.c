# include<stdio.h>
# include<readline/readline.h>
# include<readline/history.h>
# include <stdlib.h>
# include <string.h>
# include <unistd.h>

# include "libft/libft.h"
/*Esto es una prueba de como cogeriamos el promt de la terminal. Simplemente para hacer algunas pruebillas de como gestiona lo del string.*/

int	main(void)
{
	// printf("Prueba de funcion readline: char	*readline(const char *prompt)\n");
	// printf("Se utilizará como prompt: >>>\n");
	// char	*s = readline(">>>");
	// printf("el prompt de entrada ha sido: %s\n (el salto de linea no es del prompt)\n", s);
	// printf("Prueba ahora sin escribir nada, vacio, deberia devolver un string vacio\n");
	// free(s);
	// s = readline(">>>");
	// printf("el prompt de entrada ha sido: %s\n (el salto de linea no es del prompt)\n", s);
	// printf("la direccion del puntero es: %p", s);
	// free(s);
	// return (0);

	// char	*s;

	// s = readline(">>>");
	// printf("The string is :%s\n", s);
	// if (access("/bin/ls -l", F_OK | X_OK))
	// 	perror("bash");
	// else
	// 	printf("FILE FOUND\n");

	//execve()
	return (0);
}
